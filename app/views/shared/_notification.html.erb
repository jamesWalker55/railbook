<% if local_assigns[:message] and local_assigns[:color] %>
  <div class="notification is-<%= color.to_s %>">
    <%= message %>
  </div>
<% else %>
  <p>ERROR: <code>message</code> or <code>color</code> is not defined!</p>
<% end %>